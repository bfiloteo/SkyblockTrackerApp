<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.capstoneproject">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CapstoneProject">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SecondMainActivity"
            android:label="Stats Of Player"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Dungeons.DungeonsActivity"
            android:label="@string/dungeon_activity_title" />
        <activity
            android:name=".Slayers.SlayerActivity"
            android:label="@string/slayer_activity_title">

        </activity>
        <activity
            android:name=".Profile.ProfileActivity"
            android:label="@string/profile_activity_title">

        </activity>
    </application>

</manifest>